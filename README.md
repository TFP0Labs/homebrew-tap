# TFP0 Labs Homebrew Tap

Useful iOS, macOS, and Apple product reverse engineering utilities packaged for Homebrew.

## Included Formulae
 - [`gxpc`](https://github.com/ReverseApple/gxpc), a tool to sniff XPC communication using Frida and Go

## How do I install these formulae?

`brew install tfp0labs/tap/<formula>`

Or `brew tap tfp0labs/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "tfp0labs/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).

## Worthy Mentions
 - [`ipsw`](https://github.com/blacktop/ipsw), the iOS/macOS research swiss army knife
